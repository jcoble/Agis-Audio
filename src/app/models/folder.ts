export interface Folder {
    id?: number,
    user_id?: string,
    folder_name?: string,
    created_date?: Date,
    folder_type?: string,
    number_tracks?: number,
    folder_path?: string,
    child_path?: string,
    number_folders?: string
}