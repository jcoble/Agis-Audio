export interface TokenData {
    access_token?: string,
    token_type?: string,
    expires_in?: number,
    refresh_token?: string,
    userName?: string,
    first_name?: string,
    last_name?: string,
    asclient_id?: string,
    issued?: string,
    expires?: string,
    Id?: string

}